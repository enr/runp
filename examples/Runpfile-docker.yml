name: Test Runpfile
description: |
  Sample Runpfile to show a basic usage of Docker containers
units:
  container-dsl:
    description: Image crccheck/hello-world on port 8000
    container:
      image: docker.io/crccheck/hello-world
      ports:
        - "8000:8000"
