name: Test Runpfile
description: |
  Sample Runpfile to show a basic usage of Docker containers
units:
  docker-dsl:
    description: Docker image crccheck/hello-world on port 8000
    container:
      image: crccheck/hello-world
      ports:
        - "8000:8000"
